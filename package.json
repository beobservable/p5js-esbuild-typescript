{
  "name": "__ts-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle src/index.ts --outfile=public/dist/sketch.js --minify --sourcemap",
    "dev": "esbuild --bundle src/index.ts --outfile=public/dist/sketch.js --sourcemap --watch --serve=localhost:8080 --servedir=public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.11",
    "p5": "^1.6.0"
  }
}
